# Wedding Dream Backend — requirements
# Install these in your backend virtualenv

# Core framework
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3,<5.0
python-dotenv>=1.0,<2.0
django-filter>=24.2,<25.0

# Database (PostgreSQL)
psycopg2-binary>=2.9,<3.0

# Auth (JWT)
djangorestframework-simplejwt>=5.3,<6.0

# Media & storage
Pillow>=10.0,<11.0
django-storages>=1.14,<2.0
boto3>=1.34,<2.0  # if using S3; remove if not needed

# API schema/docs (optional but recommended)
drf-spectacular>=0.27,<1.0

# Caching & performance (optional)
redis>=5.0,<6.0
django-redis>=5.4,<6.0

# Async tasks (optional)
celery>=5.3,<6.0
django-celery-beat>=2.6,<3.0

# Real-time messaging (optional)
channels>=4.0,<5.0
channels-redis>=4.2,<5.0
daphne>=4.1,<5.0

# Deployment (WSGI/ASGI)
gunicorn>=21.2,<22.0  # Linux servers
whitenoise>=6.6,<7.0  # serve static files in simple setups

# Dev & testing (optional) — uncomment if you want in base env
# pytest>=8.0,<9.0
# pytest-django>=4.8,<5.0
# model-bakery>=1.19,<2.0  # or factory-boy>=3.3,<4.0
# requests>=2.32,<3.0
